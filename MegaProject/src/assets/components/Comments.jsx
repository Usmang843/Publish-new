import React from "react";

export default  Comments => {
    return(
        <>
            {/* first install pakages
            -> npm i @reduxjs/toolkit react-redux react-router-dom appwrite 
            @tinymce/tinymce-react  html-react-parser react-hook-form
            -- afterwards we need environment(System) variables
            
            */}
        </>
    );
}