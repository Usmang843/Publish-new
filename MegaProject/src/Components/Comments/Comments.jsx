import React from "react";

const Comments = () => {
  return (
    <>
      {/* We are creating react app with backend service provider appWrite
        -- npm install  @reduxjs/toolkit react-redux  react-router-dom appwrite 
        @tinymce/tinymce-react html-react-parserreact-hook-form
        -- .env crate environment variable and put all the variable in separate file to avoid returning null
        -- Authentication Service
        -- conf major configuration file of appWrite
        
        */}
    </>
  );
};

export default Comments;
