import React from "react";

const ReactL = () => {
  return (
    <>
    {/* first install node
        npm create vite@latest -> npm install -> npm run dev
        react uses bundlers 
        Hooks -> react updates and control UI 
        Diffing of lists is performed using keys, keys should be stable, predictable and unique
        Remember keys in loop in react
        Do not call useId to generate keys in a list
        Link in react-router-dom
        a tag refresh the whole page but not in react Link
        NavLink is providing additional properties as from href -> To
    */}
    </>
  );
}
export default ReactL;